# AGENTS.md

## 项目定位

NetworkExpression 是面向计算机网络原理初学者的中文交互式学习网站。目标是把抽象的协议流程拆成可操作、可观察、可解释的步骤，帮助学习者建立“谁向谁发送了什么、为什么、下一步会怎样”的心智模型。

## 交付要求

- 网站可在本地直接启动，不依赖账号、后端或外部 API。
- 学习路径包含分层封装、DNS、TCP、HTTP、ARP、DHCP、IPv4 路由转发和 ICMP Echo；新增场景继续沿用同一播放模型。
- 每个演示提供清晰的起点、上一步/下一步/重置操作，以及当前步骤的可视化和文字解释。
- 每个场景提供一道概念自测题；学习进度仅保存在浏览器本地。概念速查可跳转到对应演示。
- 界面以中文为主，兼顾桌面与手机；控件应能通过键盘操作，动态说明应对辅助技术可读。
- 教学内容要区分“教学简化”和真实协议行为，不能用动画表现误导性的因果关系。
- 新增场景优先通过数据描述接入，不复制整套页面逻辑。

## 仓库约定

- `index.html`：页面骨架和元信息。
- `src/main.js`：界面渲染与交互。
- `src/lessons.js`：场景与步骤内容；保持数据结构一致。
- `src/lessons-extended.js`：扩展场景数据。
- `src/learning.js`：概念、自测、目录筛选与本地进度逻辑。
- `src/styles.css`：布局、视觉样式与响应式规则。
- `scripts/dev-server.js`：无第三方依赖的本地静态服务器。
- `tests/`：针对场景数据和关键交互状态的轻量测试。

## 内容和实现原则

1. 先保证协议表述正确，再追求视觉效果。使用示例地址或明确标注的示意数据。
2. 一步只讲一个主要变化；步骤标题、报文、说明和画面应相互对应。
3. 不把示意图说成真实抓包结果。涉及 DNS、HTTP 等协议时，注明省略的缓存、连接复用、TLS 等条件。
4. 保持前端可离线运行；不要为简单演示引入不必要的框架或远程资源。
5. 新增场景时同步补充自测题、分层分类、示意假设及必要的原始标准来源。
6. 修改场景时同步检查步骤边界、移动端布局和键盘操作；运行 `npm test`。

## 当前完成标准

用户打开首页后，能在八个场景之间切换和查找；每个场景可完成逐步演示、自测和本地进度记录，并看懂当前参与者、传输方向、报文和对应的原理说明。`npm test` 通过，`npm run dev` 可启动网站。
